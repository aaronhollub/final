<% gantt = Gantt.find_by("id" => params["id"]) %>

<h1>Edit Gantt Chart #<%= gantt["id"] %></h1>

<%= form_tag "/gantts#{gantt["id"]}", "method" => "patch" %>

  <p>
    <%= label_tag "url", "What is the URL for the Gantt chart?" %>
    <%= text_field_tag "url", gantt["gantt_url"] %>
  </p>

  <p>
    <%= label_tag "project", "What project is this chart associated with?" %>
    <%= select_tag "project_name", options_from_collection_for_select(Project.read.order("project_name"), "project_name", "project_name"), "class" => "form-control" %>
  </p>

  <%= submit_tag %> or <%= link_to "Cancel", "/projects" %>

</form>
