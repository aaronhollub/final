<% gantt = Update.find_by("id" => params["id"]) %>

<h2>Edit Your Update></h2>

<%= form_tag "/updates/#{update["id"]}", "method" => "patch" %>

  <div class="form-group">
    <label class="col-sm-2 control-label">Update</label>
    <div class="col-sm-10">
      <input type="text" class="form-control" name="summary" value="<%= update["summary"] %>">
    </div>
  </div>

<!--   <div class="form-group">
    <label class="col-sm-2 control-label">Project Name</label>
    <div class="col-sm-10">
    <%= select_tag "project_name", options_from_collection_for_select(Project.read.order("project_name"), "project_name", "project_name"), "class" => "form-control" %>
    </div>
  </div>
 -->
<%= submit_tag %> or <%= link_to "Cancel", "/admin" %>
